"""Reusable scripts that generate the publication-quality figures
shown in the CALLIOPE Tutorials section of the docs.

One script per tutorial page, plus a shared `_style` module that
reuses the Roboto font registration and helper functions from the
cross-backend comparison scripts so all docs figures share one look.
"""
